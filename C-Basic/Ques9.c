// Q. calculate the circumference of circle.

#include<stdio.h>
int main(){
    int radius;
    printf("Enter the radius the circle of radius:");
    scanf("%d",&radius);
    float circumfarence = 2*3.14*radius;
    printf("circumfarence of the Circle: %.2f",circumference);
    return 0;
}